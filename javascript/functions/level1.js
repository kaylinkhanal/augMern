const name ="hello"
function test(){
    return name
}

//Q1 convert to arrow function

const test = (name) =>  {
    return name;
  }
  test('hello')
//Q2 pass name as argument instead of external constants