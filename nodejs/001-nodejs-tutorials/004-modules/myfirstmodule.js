// exports.myDateTime = function () {
// 	return Date();
// };

/**
 * Returns the current date and time as a string.
 * This function fetches the current date and time from the system and returns it in a human-readable format.
 * @returns {string} The current date and time.
 */
exports.myDateTime=()=>{
	return Date()
}